<view>
  <button bindtap='upload' class='btn' plain='true' type='primary'>Upload ↑</button>
  <button bindtap='download' class='btn' plain='true' type='warn'>Download ↓</button>

  <view class='image'>
    <image src='{{image}}' class='img'></image>
  </view>
    
</view>